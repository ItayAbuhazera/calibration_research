# GeometricInternalCalibration - base requirements
#
# This file intentionally contains only the packages needed for the default
# IJCAI reproduction path and for a portable `pip install -r requirements.txt`
# workflow in a fresh environment.

# Core scientific stack
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
tabulate>=0.9.0
Pillow>=9.5.0
PyYAML>=6.0

# Machine learning
scikit-learn>=1.2.0
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0
transformers>=4.38.0

# Optional / not required for the default paper pipeline:
# - seaborn
# - submitit
# - faiss-cpu / faiss-gpu
